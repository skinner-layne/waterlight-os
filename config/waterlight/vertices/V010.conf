# V010 -- Neutron
# Core kernel services
# epsilon=0 (kernel), mu=1 (heavyweight), sigma=0 (production)

[vertex]
id = V010
name = Neutron
epsilon = 0
mu = 1
sigma = 0
particle = neutron
chiral_partner = V011

[cgroup]
slice =
enforce = false

[description]
summary = Core kernel services (scheduler, memory manager, VFS, network stack)
components = CFS/EEVDF scheduler, page allocator, slab, VFS, block I/O, network stack, device model
