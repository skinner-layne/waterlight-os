# V011 -- Antineutron
# Kernel instrumentation and crash analysis
# epsilon=0 (kernel), mu=1 (heavyweight), sigma=1 (development)

[vertex]
id = V011
name = Antineutron
epsilon = 0
mu = 1
sigma = 1
particle = antineutron
chiral_partner = V010

[cgroup]
slice =
enforce = false

[description]
summary = Kernel instrumentation, crash dumps, and sanitizers
components = kdump, kgdb, KASAN, KCSAN, KMEMLEAK, lockdep
