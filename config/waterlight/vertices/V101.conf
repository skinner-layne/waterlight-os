# V101 -- Antiphoton
# Lightweight development tools
# epsilon=1 (user), mu=0 (lightweight), sigma=1 (development)

[vertex]
id = V101
name = Antiphoton
epsilon = 1
mu = 0
sigma = 1
particle = antiphoton
chiral_partner = V100

[cgroup]
slice = antiphoton
enforce = true
memory_high = 64M
memory_max = 128M
memory_swap = 0
cpu_weight = 25
pids_max = 256

[capabilities]
granted = cap_net_bind_service cap_sys_ptrace
ambient = cap_net_bind_service
denied = cap_sys_admin

[namespaces]
pid = shared-with-photon
net = shared-with-photon
mnt = private-with-debug
uts = private
ipc = private
user = mapped
cgroup = private

[description]
summary = Lightweight development tools (watchers, linters, language servers)
components = entr, watchexec, shellcheck, shfmt, LSP servers, hot-reload proxies
