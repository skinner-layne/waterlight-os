# V000 -- Neutrino
# Kernel microthreads, zero-overhead watchers
# epsilon=0 (kernel), mu=0 (lightweight), sigma=0 (production)

[vertex]
id = V000
name = Neutrino
epsilon = 0
mu = 0
sigma = 0
particle = neutrino
chiral_partner = V001

[cgroup]
# Accounting only -- no enforcement for kernel threads
slice = neutrino
enforce = false

[description]
summary = Kernel microthreads and zero-overhead watchers
components = kworker, softirq handlers, RCU callbacks, timer interrupts
